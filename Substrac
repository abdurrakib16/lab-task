#include <stdio.h>
int main()
{
    int num1, num2, sub;
    printf("Enter two number: ");
    scanf("%d %d", &num1, &num2);

    if(num1<=num2){
        sub=num2-num1;
    } else{
        sub=num1-num2;
    }
    printf("Sub is: %d", sub);
}
